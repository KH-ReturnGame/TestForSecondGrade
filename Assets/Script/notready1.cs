//오류메세지  구현 실패
/*
using UnityEngine;
using System.Collections;
public class notready1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Button.active = true;
        Invoke("Hide", 3);
    }
    
    void Hide()
    {
        Button.actvie = false;
    }
}
*/